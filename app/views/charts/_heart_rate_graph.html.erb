<div class="row">
  <div id="heart_container" style="width:100%; height:300px;"></div>
  <script type="text/javascript">
    $(function () { 
        $('#heart_container').highcharts({
            plotOptions: {
              series: {
                animation: false
              }
            },

            chart: {
                animation: false
            },
            title: {
                text: 'Heart Rate(BPM)'
            },
            xAxis: {
              categories: <%=raw @observation_set.map {|b| b.created_at.strftime("%H%M") } %>
            },
            series: [
              {
                showInLegend: false,               
                data: <%=raw @observation_set.map {|b| [b.created_at.strftime("%H%M"), b.heart_rate] } %>
              }
            ]
        });
    });
  </script>
</div>
