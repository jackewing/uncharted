<div class="row">
  <div id="o2_sat_container" style="width:100%; height:200px;"></div>
  <script type="text/javascript">
    $(function () { 
      $('#o2_sat_container').highcharts({
        plotOptions: {
          line: {
            dataLabels: {
              enabled: true
            },
          },
            series: {
              animation: false
            }
          },

            chart: {
                height: 200,
                animation: false,
            },
            title: {
                text: 'O2 Saturation %'
            },
            yAxis: {
              max: 100,
              min: 85
            },
            xAxis: {
              categories: <%=raw @observation_set.map {|b| b.created_at.strftime("%H%M") } %>,
              minPadding: 0,
              maxPadding: 0,
            },
            series: [
              {
                showInLegend: false,               
                data: <%=raw @observation_set.map {|b| [b.created_at.strftime("%H%M"), b.o2_saturation] } %>,
              }
            ]
        });
    });
  </script>
</div>
